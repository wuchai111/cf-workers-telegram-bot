{
	"name": "root",
	"private": true,
	"author": "codebam",
	"license": "Apache-2.0",
	"scripts": {
		"build": "lerna run build",
		"build:worker": "lerna run build --scope=worker",
		"test": "lerna run test",
		"test:worker": "lerna run test --scope=worker",
		"test:main": "lerna run test --scope=@codebam/cf-workers-telegram-bot",
		"release": "lerna version minor -y",
		"publish": "lerna publish -y from-git",
		"lint": "lerna run lint"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@types/marked": "^6.0.0",
		"@typescript-eslint/eslint-plugin": "^7.8.0"
	},
	"dependencies": {
		"marked": "^12.0.2"
	}
}
